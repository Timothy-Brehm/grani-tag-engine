# @grani/schema-tools

Workspace helpers for JSON Schema: types, AJV compile/validate, local `$ref` resolution, MVP generation, and validation message formatting.

Private package for now (`private: true`).
